/* 
 * {{module_name}}
 * @author {{author}}
 * @version {{version}}
 *
 * @param {TYPE} NAME - DESCRIPTION
 * @returns {TYPE} - DESCRIPTION
 */

import sum from '{{module_name}}.sub.mod';

console.log('The result is nothing else then ', sum([1,2]));
